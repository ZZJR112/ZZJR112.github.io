# Render 部署配置：把本仓库作为 Node Web Service 部署
# server.js 零依赖，直接 node server.js 启动；Render 会自动注入 PORT 环境变量
services:
  - type: web
    name: zjr-portfolio
    runtime: node
    plan: free
    branch: master
    region: oregon
    startCommand: "npm start"
    healthCheckPath: "/"
